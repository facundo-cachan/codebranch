import { StyleSheet } from 'react-native';

const btnStyles = StyleSheet.create({
  container: {
    borderColor: '#000',
    borderRadius: 10,
    borderWidth: 1,
    flex: 1,
  },
});

export default btnStyles;
